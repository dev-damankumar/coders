import { env } from "./utils";

export default {
  baseImageSrc: env["REACT_APP_BASE_URL"],
  baseURL: env["REACT_APP_BASE_URL"],
};

export const baseImageSrc = env["REACT_APP_BASE_URL"];
export const baseURL = env["REACT_APP_BASE_URL"];
